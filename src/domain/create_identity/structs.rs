pub struct CreateIdentityRequest {
    pub alias: String,
    pub hostname: String,
    pub config_path: String,
}

pub struct CreateIdentityResponse {
    pub alias: String,
    pub hostname: String,
    pub config_path: String,
}